--Steps to install pythereum on mac--

1.) Clone the repository - https://github.com/ethereum/pyethereum.wiki.git
2.) Install the osX dependencies - brew install pkg-config libffi autoconf automake libtool openssl
3.) Run - python setup.py install
4.) Install all the dependencies in requirements.txt and dev-requirements.txt using, pip install -r <requirements file>
